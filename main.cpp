#include <iostream>
using namespace std;

int main() {
    // Приветственное сообщение
    cout << "Hello, World!" << endl;

    // Пример изменения
    int a = 5, b = 3;
    int sum = a + b;

    // Вывод суммы
    cout << "Sum of " << a << " and " << b << " is " << sum << endl;

    // Дополнительная логика или изменения могут быть добавлены сюда
    return 0;
}
